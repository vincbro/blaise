![ontrack](../../assets/ontrack.png)

ontrack server is a server meant for internal use (not public access) for
projects that cannot directly interact with the rust library, as of now it supports search,
near and routing as of now but the goal
is near 100% mapping of the library to the endpoints of the server.

